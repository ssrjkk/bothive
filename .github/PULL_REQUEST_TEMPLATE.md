## What & why

Briefly describe what this PR changes and the problem it solves.

## How it was verified

- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes (all workspaces)
- [ ] Added/updated tests for new or changed behaviour

## Impact

- [ ] New or changed API endpoints → `docs/api.md` and README API surface updated
- [ ] Safety-sensitive areas (script sandbox, SSRF, credential handling, RBAC) → unsafe paths are covered by tests
- [ ] Dashboard UI → uses theme tokens (works in light and dark), no hardcoded colors

## Notes

Anything reviewers should know — trade-offs, follow-up work, screenshots.
