{
  "name": "@bothive/core",
  "version": "1.0.0",
  "author": "ssrjkk",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "BotHive Core — shared types, interfaces, and utilities by ssrjkk",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc",
    "dev": "tsc --watch",
    "lint": "eslint ."
  },
  "dependencies": {
    "@sentry/node": "^10.69.0",
    "ioredis": "^6.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
